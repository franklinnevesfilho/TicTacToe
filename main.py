import game_window as window

if __name__ == "__main__":
    window.start()  # Create an instance of the Window class
    # The Tkinter main loop is started in the Window class constructor
    # so we don't need to do anything else here.  # The program will run until the window is closed.